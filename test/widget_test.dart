// Main widget_test.dart — individual feature tests are in test/features/
// This file verifies the test suite can be discovered.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite is discoverable', () {
    expect(true, isTrue);
  });
}
